# Local development profile (IDE / IntelliJ)
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: true

logging:
  level:
    com.atlaspay: DEBUG
    org.hibernate.SQL: DEBUG
